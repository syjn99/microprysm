#!/bin/bash
# Re-inject key context after compaction.

cat <<'MSG'
Reminder:
- Go tests require -tags=develop
- Use testing/assert (non-fatal) and testing/require (fatal), not testify
- Run /precheck before committing
- Forks: Phase0 → Altair → Bellatrix → Capella → Deneb → Electra → Fulu → Gloas
MSG
